// ============================================================================
// wifi_static.h - Работа с WiFi (статический IP)
// ============================================================================
// Проект: Heat-Chamber
// Описание: Простое подключение к WiFi с фиксированным IP
// ============================================================================

#ifndef WIFI_STATIC_H
#define WIFI_STATIC_H

#include <Arduino.h>
#include <WiFi.h>
#include "config.h"

// ============================================================================
// ФУНКЦИИ
// ============================================================================

/**
 * Подключение к WiFi со статическим IP из config.h
 * @return true - успешно, false - ошибка
 */
bool initWiFi();

/**
 * Проверка статуса подключения
 * @return true - WiFi подключён
 */
bool isWiFiConnected();

/**
 * Получение текущего IP-адреса
 * @return String - IP-адрес
 */
String getWiFiIP();

/**
 * Перезагрузка ESP32
 * @param reason - причина перезагрузки (для отладки)
 */
void restartESP(const char* reason);

#endif // WIFI_STATIC_H