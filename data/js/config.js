// ============================================
// КОНФИГУРАЦИЯ
// ============================================

// ============================================================================
// ⚠️ ВНИМАНИЕ: Здесь можно менять только то, что помечено "МОЖНО МЕНЯТЬ"
// ============================================================================

const CONFIG = {
    // --- НАСТРОЙКИ БУФЕРА (МОЖНО МЕНЯТЬ, но осторожно) ---
    MAX_POINTS: 2700,  // Максимальное количество точек в буфере графика
                       // 2700 = 45 минут при обновлении 1 раз в секунду
                       // Можно увеличить до 5400 (90 минут), но следи за памятью!
    
    // --- WebSocket (НЕ ТРОГАТЬ) ---
    WS_URL: 'ws://' + window.location.hostname + ':8080',
    
    // --- НАСТРОЙКИ КНОПОК МАСШТАБА (МОЖНО МЕНЯТЬ) ---
    RANGES: {
        '10': 10,        // 10 минут (ключ '10' = 10 минут)
        '30': 30,        // 30 минут (ключ '30' = 30 минут)
        '45': 45,        // 45 минут (ключ '45' = 45 минут)
        'half': 'half',  // половинное сжатие (ключ 'half' = специальный режим)
        'quarter': 'quarter' // четвертное сжатие (ключ 'quarter' = специальный режим)
    },
    
    // --- Период обновления графика (НЕ ТРОГАТЬ) ---
    UPDATE_INTERVAL: 1000,  // 1000 мс = 1 секунда (синхронизировано с ESP)
    
    // --- НАСТРОЙКИ ПЕРЕПОДКЛЮЧЕНИЯ (МОЖНО МЕНЯТЬ) ---
    RECONNECT: {
        MAX_ATTEMPTS: 5,     // Сколько раз пытаться переподключиться
        BASE_DELAY: 1000,    // Начальная задержка (1 секунда)
        MAX_DELAY: 30000     // Максимальная задержка (30 секунд)
    },
    
    // --- НАСТРОЙКИ KEEPALIVE (МОЖНО МЕНЯТЬ) ---
    KEEPALIVE_INTERVAL: 20000,  // Как часто отправлять ping (20 секунд)
    
    // --- ТАЙМАУТ ДАННЫХ (МОЖНО МЕНЯТЬ) ---
    DATA_TIMEOUT: 10000  // Через сколько секунд без данных считать соединение потерянным (10 секунд)
};

// ============================================================================
// ✅ КОРОТКО: ЧТО МОЖНО МЕНЯТЬ
// ============================================================================
// 1. MAX_POINTS     - размер буфера (2700 = 45 мин, 5400 = 90 мин)
// 2. RANGES         - можно добавлять/удалять кнопки масштаба
// 3. RECONNECT      - настройки переподключения
// 4. KEEPALIVE_INTERVAL - частота ping
// 5. DATA_TIMEOUT   - таймаут данных
//
// ⚠️ НЕ МЕНЯТЬ: WS_URL, UPDATE_INTERVAL - это критично для связи с ESP!
// ============================================================================